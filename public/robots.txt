User-agent: *
Allow: /

# Dashboard, order, admin, and loyalty views require a logged-in session
# and are rendered entirely client-side with no dedicated public path --
# nothing to disallow here for them. All other sections now have real,
# distinct URLs (see sitemap.xml) served by the same index.html via the
# server's SPA fallback (try_files ... /index.html).

Sitemap: https://inmisr.net/sitemap.xml
