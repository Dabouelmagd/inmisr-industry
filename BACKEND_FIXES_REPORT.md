# تقرير إصلاحات الباك إند — جلسة بناء نظام المصادقة الحقيقي

تاريخ: يونيو 2026
الهدف الأصلي: نظام مصادقة حقيقي (إيميلات SendGrid، قاعدة بيانات، تشفير باسورد)

## ٠. تحديث لاحق: الرجوع لـ SMTP (SiteGround) بدل SendGrid

بعد المراجعة، اتضح إن SendGrid شال الباقة المجانية الدائمة نهائيًا (بقت تجربة
60 يوم بس)، وفيه احتمال رفض حساب بدون سبب واضح. بما إن عندك حساب SiteGround
شغّال فعليًا بخدمة بريد مجانية ضمنه (تكفي تأكيد تسجيل + نسيان باسورد لحجم
الاستخدام الحالي)، تم الرجوع لـ Nodemailer/SMTP عمدًا:

- `notifications.service.ts`: استخدام `nodemailer` بدل `@sendgrid/mail`
- `.env.example` / `.env.production`: متغيرات `SMTP_HOST`, `SMTP_PORT`,
  `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM_EMAIL`, `SMTP_FROM_NAME` بدل
  `SENDGRID_API_KEY`
- بيانات SiteGround المتوقعة: `SMTP_HOST=mail.inmisr.net`, `SMTP_PORT=465`
  (SSL) أو `587` (TLS), `SMTP_USER`/`SMTP_PASS` = بيانات حساب الإيميل على
  لوحة SiteGround (Site Tools → Email → Accounts)

ملاحظة مهمة: SiteGround SMTP محدود بـ 400-800 إيميل/ساعة (حسب الباقة)، وهو
مناسب تمامًا لحجم الاستخدام الحالي (تأكيدات + استعادة باسورد)، لكن لو
المنصة كبرت لاحقًا واحتجتي آلاف الإيميلات يوميًا، وقتها فكّري في الرجوع
لـ SendGrid أو بديل مثل Brevo (300 إيميل/يوم مجاني دائم).

---

## ملخص النتيجة

الباك إند (NestJS) كان موجوداً من جلسة سابقة بكمية كبيرة من الكود المحترف
(bcrypt، JWT، OTP، 2FA، audit log) — لكنه **لم يكن قابلاً للتشغيل أو الـ compile
على الإطلاق** بسبب أخطاء بنيوية حقيقية سابقة لهذه الجلسة. تم اكتشافها وإصلاحها
بالكامل، بالإضافة لتنفيذ طلبك الأصلي (SendGrid + قاعدة بيانات حقيقية).

تم التحقق عمليًا عبر:
1. `npm install` فعلي (794 حزمة)
2. `npx tsc --noEmit` فعلي على الكود الحقيقي

النتيجة: من **38 مسار استيراد مكسور بالكامل** (المشروع لم يكن يعمل من الأساس)
إلى **0 مسار مكسور**، ومن عدم القدرة على الـ compile إطلاقًا إلى compile نظيف
100% فيما عدا 11 خطأ مرتبط حصريًا بعدم توفر شبكة لتوليد Prisma Client في بيئة
العمل الحالية (سيُحل أوتوماتيكيًا على VPS حقيقي بإنترنت طبيعي).

---

## ١. تنفيذ الطلب الأصلي: نظام مصادقة حقيقي

### قاعدة بيانات بدل الذاكرة
- إضافة جدول `PasswordResetToken` في `prisma/schema.prisma` (كان التوكن
  محفوظ في `Map` بالذاكرة فقط — يضيع بمجرد إعادة تشغيل السيرفر)
- إعادة كتابة `password-reset.service.ts` بالكامل ليستخدم قاعدة البيانات،
  مع تخزين hash للتوكن (SHA-256) بدل القيمة الخام، حماية إضافية لو تسرّبت
  قاعدة البيانات

### SendGrid بدل Nodemailer/SMTP
- `notifications.service.ts`: استبدال `nodemailer` بـ `@sendgrid/mail` SDK
  رسمي، مع `onModuleInit()` يتحقق من وجود `SENDGRID_API_KEY` ويحذّر لو غير
  موجود
- `.env.example` و `.env.production`: حذف متغيرات SMTP القديمة، إضافة
  `SENDGRID_API_KEY` و `SENDGRID_FROM_EMAIL`
- `docker-compose.yml`: تحديث تمرير متغيرات البيئة للحاوية بنفس التغيير

### تحديث الهوية البصرية والدومين
- استبدال كل إشارات `inmisr.com` بـ `inmisr.net` (~30 موضع عبر 15 ملف:
  خدمات، قوالب إيميل، seed scripts، Docker/K8s، CI/CD، docs)
- تحديث ألوان قوالب الإيميل من الأحمر/الأسود القديم (`#CE1126`/`#0D0D0D`)
  للذهبي الجديد (`#D4A017`/`#0D0F12`) ليطابق تصميم المنصة الحالي

---

## ٢. أخطاء بنيوية حرجة كانت موجودة من قبل (غير متعلقة بطلبك، تم اكتشافها
   وإصلاحها أثناء العمل)

هذه الأخطاء كانت تمنع المشروع من العمل **حتى قبل أي تدخل من هذه الجلسة**:

### `app.module.ts` (الملف الذي يربط كل أجزاء النظام)
كان فيه **~30 مسار استيراد خاطئ بالكامل** — يشير لمجلدات غير موجودة
(مثلاً `./twofa/twofa.service` بينما الملف الحقيقي في
`src/auth/twofa.service.ts`). تمت إعادة بناء الملف بالكامل من الصفر بعد
فحص شامل ومتحقق منه لموقع كل خدمة وكل controller فعليًا في المشروع.

### ملفات فيها محتوى مكرر/ملتصق بالغلط
- `main.ts`: كان فيه كود controllers كامل (بحث وخرائط) ملتصق فوق كود
  الـ bootstrap الحقيقي — تمت إزالة الجزء الزائد والاحتفاظ بالنسخة
  الصحيحة الموجودة في `search.controller.ts`
- `jwt.guard.ts` و `search.controller.ts`: استيرادات مكررة لنفس الأسماء
  (Injectable، Controller، إلخ) من دمج خاطئ لملفين في واحد

### مسارات استيراد خاطئة منتشرة (8+ ملفات)
كل الملفات اللي بتستورد `JwtGuard` كانت بمسار خاطئ (`../guards/jwt.guard`
بدل `../auth/jwt.guard`)، وكذلك `EncryptionService` في ملفين، و
`TwoFaService`، و `InvoiceService`، و `ReportsService`، و `MapsService`.

### تبعيات (dependencies) ناقصة أو غير صحيحة
- `multer@^1.4.5` — **هذا الإصدار غير موجود على npm registry إطلاقًا**
  (كان سيفشل `npm install` من اللحظة الأولى)؛ تم تثبيته على
  `1.4.5-lts.2` (الإصدار الآمن المتاح)
- `cookie-parser`, `qrcode`, `otplib`, `@sentry/node` — مستخدمة في الكود
  بدون أي وجود في `package.json`

### تعارض إصدارات مكتبات (type mismatches حقيقية)
- `amqplib`: الكود كان يستخدم type قديم (`Connection`) بينما النسخة
  المثبتة الحديثة بترجع `ChannelModel`
- `@elastic/elasticsearch`: الكود مكتوب بطراز v7 (`body: {...}` wrapper)
  بينما النسخة المثبتة v8 تتطلب الحقول مباشرة بدون `body`

### كنترولر مكرر
`CategoriesController` كان معرّف مرتين — نسخة stub فاضية في
`app/controllers.ts` (تحتوي تعليق `// Will be injected properly in
module`)، ونسخة حقيقية كاملة في `products.service.ts`. تم حذف الـ stub.

---

## ٣. ما لم يكتمل ولماذا (شفافية كاملة)

### Prisma Client غير مولَّد
بيئة العمل الحالية (sandbox) محظور عليها الوصول لـ `binaries.prisma.sh`
(مطلوب لتحميل محرك Prisma). هذا يعني 11 خطأ TypeScript متبقي، وكلها
مرتبطة حصريًا بهذا — تم التأكد من أن كل enum مذكور (`Role`, `RfqStatus`,
`SubscriptionPlan`, `PaymentMethod`, `VerifiedLevel`) **موجود بالفعل**
في `schema.prisma`. **على VPS حقيقي بإنترنت طبيعي، شغّلي:**
```bash
npx prisma generate
```
وستختفي هذه الأخطاء فورًا.

### otplib أُرجع لإصدار v12 عمدًا
الإصدار v13 الحالي على npm به إعادة هيكلة جذرية لواجهة الـ API (الكلاس
بقى لازم تعمل `instantiate` بدل singleton جاهز، والـ `verify()` بقى
`async`). هذا يحتاج إعادة كتابة جزء من `twofa.service.ts` نفسه — خارج
نطاق "تشغيل نظام المصادقة"، فتم تثبيت v12 (يعمل صحيح، فيه تحذير
deprecation بس بدون مشاكل وظيفية).

### لا يوجد ملفات اختبار (.spec.ts)
لاحظت عدم وجود أي ملف اختبار في المشروع كله. هذا أمر يُفضّل معالجته
قبل الإطلاق الفعلي لكنه خارج نطاق هذه الجلسة.

### تطبيق الموبايل (React Native)
ملف `mobile/src/client.ts` يحتوي على كود JSX محفوظ بامتداد `.ts` بدل
`.tsx`، مما يجعله غير قابل للـ parsing. تم استثناء مجلد `mobile/` من
فحص TypeScript الخاص بالباك إند (لأنه مشروع منفصل بأدوات بناء مختلفة –
Expo/Metro)، لكن هذا الملف نفسه يحتاج إصلاح منفصل لو احتجتي تطوير
تطبيق الموبايل.

---

## ٤. الخطوات التالية للنشر على Hetzner VPS

1. رفع الملفات: `scp -r inmisr-complete/ user@VPS_IP:/var/www/inmisr`
2. `npm install` على السيرفر (هيثبت كل التبعيات بشكل طبيعي)
3. نسخ `.env.example` لـ `.env` وتعبئة القيم الحقيقية (مفتاح SendGrid،
   بيانات قاعدة البيانات، JWT secrets، إلخ)
4. `npx prisma generate` (سينجح على VPS عادي بإنترنت طبيعي)
5. `npx prisma migrate deploy` لإنشاء جداول قاعدة البيانات
6. `npm run build` ثم `npm run start` (أو استخدام `docker-compose up`
   الموجود في المشروع للنشر بالكامل عبر Docker)

ملف `scripts/setup-server.sh` الموجود بالمشروع يحتوي على الخطوات
التفصيلية لإعداد سيرفر Ubuntu من الصفر (PostgreSQL، Redis، Nginx،
SSL، إلخ).
